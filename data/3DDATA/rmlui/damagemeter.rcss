/* Damage meter -- LAYOUT ONLY.

   Colours, bevels, gloss, button states and scrollbars all come from
   rose-theme.rcss, which the document links first. If a rule here starts
   choosing a colour or a gradient, it probably belongs in the theme so the
   next panel gets it too. Reload in game with /uireload. */

/* --- window -------------------------------------------------------------- */
#meter {
    left: 40px;
    top: 180px;
    width: 400px;
}

/* --- info strip ------------------------------------------------------------ */
.infobar {
    display: flex;
    flex-direction: row;
    align-items: center;
    height: 20px;
    padding-left: 6px;
    padding-right: 8px;
    margin-bottom: 4px;
}

.info-left  { flex-grow: 1; }
.info-right { text-align: right; }

/* A lit pip in front of the timer while a fight is running. The only colour
   rule in this file, because it is this panel's own state, not a material. */
.live-dot {
    width: 6px;
    height: 6px;
    margin-right: 6px;
    border-radius: 3px;
    background-color: #ffffff20;
}
.live-dot.on { background-color: #ff7a30ff; }

/* --- rows ------------------------------------------------------------------ */
.rows {
    max-height: 244px;         /* ten rows fit without a scrollbar */
    overflow-y: auto;
    padding: 3px;
}

.row {
    display: flex;
    flex-direction: row;
    align-items: center;
    height: 22px;
    margin-bottom: 2px;
}

.rank {
    width: 30px;
    text-align: center;
    color: #9a9a9a;
}

.name {
    width: 96px;
    padding-right: 6px;
    font-weight: bold;
    overflow: hidden;
    white-space: nowrap;
    text-overflow: ellipsis;
}

/* The avatar's own row, whichever place it is in. */
.row.self .name,
.row.self .rank { color: #ff9a5a; }

/* --- the bar ---------------------------------------------------------------- */
.bar {
    flex-grow: 1;
    height: 18px;

    display: flex;
    flex-direction: row;
    align-items: center;
    padding-left: 6px;
    padding-right: 6px;
}

.dps { width: 92px; text-align: right; }
.dmg { width: 84px; text-align: right; color: #d8d8d8; }
.pct { flex-grow: 1; text-align: right; }

/* --- footer ------------------------------------------------------------------ */
.statusline {
    height: 16px;
    margin-top: 4px;
    padding-left: 4px;
}

.footer {
    display: flex;
    flex-direction: row;
    justify-content: center;
    margin-top: 2px;
    margin-bottom: 1px;
}

.footer .ui-btn {
    width: 72px;
    margin-left: 3px;
    margin-right: 3px;
}
